import { QueryClient } from "@tanstack/react-query";

// Singleton QueryClient for the app.
// Replace with your preferred configuration if needed.
export const queryClientInstance = new QueryClient();

